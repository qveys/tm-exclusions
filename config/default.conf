# ════════════════════════════════════════════════════════════════
#  tm-exclusions — default.conf
#  Built-in exclusion rules for developer Macs.
#  Criterion: regenerable content (cache, install, build, models…).
#
#  Format: type|target|reason
#    type    = path | pattern | prune
#    target  = $HOME/... (preferred) or absolute path
#    reason  = short human-readable note shown in report
#
#  Variables: $HOME expanded at load time (~ also accepted, legacy).
#  Path globs: path targets may contain * ? [ ] metacharacters; they are
#  expanded at load time. Unmatched globs are ignored (not passed to tmutil).
#  Sections: lines starting with `#@` are treated as comments by the 1.x
#  loader; they are cosmetic today and reserved for future category-aware
#  reporting (#34).
# ════════════════════════════════════════════════════════════════

# ── STATIC EXCLUSIONS (path) ─────────────────────────────────

#@Applications
path|/Applications|Apps — reinstallable
path|$HOME/Applications|User apps

#@Node.js / JavaScript
path|$HOME/.npm|npm cache and config
path|$HOME/.yarn/cache|Yarn cache
path|$HOME/.yarn/berry/cache|Yarn Berry cache
path|$HOME/.pnpm-store|pnpm store
path|$HOME/Library/pnpm|pnpm global
path|$HOME/.local/share/pnpm|pnpm data
path|$HOME/.bun|Bun runtime + packages
path|$HOME/.nvm|nvm versions

#@Python
path|$HOME/.cache/pip|pip download cache
path|$HOME/Library/Python|macOS system Python — site-packages
path|$HOME/.local/share/virtualenvs|pipenv envs
path|$HOME/.local/share/uv|uv tools + cache
path|$HOME/.uv|uv config/data
path|$HOME/.local/pipx|pipx venvs
path|$HOME/.pyenv/versions|pyenv versions
path|$HOME/.conda|Conda envs
path|$HOME/miniconda3|Miniconda
path|$HOME/anaconda3|Anaconda
path|$HOME/miniforge3|Miniforge

#@Docker
# $HOME/.docker is intentionally kept in backups: it is small and contains
# config.json (registry auth tokens). Bulky data is covered by the rules below.
path|$HOME/Library/Containers/com.docker.docker|Docker Desktop data
path|$HOME/Library/Group Containers/group.com.docker|Docker group containers
path|$HOME/Library/Application Support/Docker Desktop|Docker Desktop support

#@Homebrew
path|/opt/homebrew|Homebrew (Apple Silicon) — reinstallable via brew bundle
path|/usr/local/Cellar|Homebrew Cellar (Intel)
# $HOME/Library/Caches/Homebrew is covered by the macOS Caches parent rule.

#@Rust / Cargo
path|$HOME/.cargo/registry|Cargo crate registry
path|$HOME/.cargo/git|Cargo git sources
path|$HOME/.rustup/toolchains|rustup toolchains

#@Java / JVM
path|$HOME/.m2/repository|Maven cache
path|$HOME/.gradle/caches|Gradle cache
path|$HOME/.gradle/wrapper|Gradle wrapper distributions
path|$HOME/.ivy2/cache|Ivy cache
path|$HOME/.sbt|SBT cache
path|$HOME/.coursier/cache|Coursier cache

#@Go
path|$HOME/go/pkg/mod|Go module cache
path|$HOME/.cache/go-build|Go build cache

#@Ruby / iOS
path|$HOME/.rbenv/versions|Ruby rbenv versions
path|$HOME/.rvm|RVM
path|$HOME/.gem|Ruby gems
path|$HOME/.cocoapods|CocoaPods repo

#@Xcode / Apple Dev Tools
path|$HOME/Library/Developer/Xcode/DerivedData|DerivedData — auto-rebuilt
path|$HOME/Library/Developer/Xcode/Archives|Xcode archives
path|$HOME/Library/Developer/Xcode/iOS DeviceSupport|iOS Device Support
path|$HOME/Library/Developer/Xcode/watchOS DeviceSupport|watchOS Device Support
path|$HOME/Library/Developer/Xcode/tvOS DeviceSupport|tvOS Device Support
path|$HOME/Library/Developer/Xcode/visionOS DeviceSupport|visionOS Device Support
path|$HOME/Library/Developer/Xcode/Products|Xcode build products
# Exclude bulky CoreSimulator subtrees, not the parent directory: root-level
# plists and similar metadata stay in backups. Comment out Devices in a local
# catalog copy (or TM_EXCLUSIONS_DEFAULT_CONF) to keep simulator device state
# while still excluding caches/temp/volumes. custom.conf is additive only.
# Apply/dry-run/uninstall drop a leftover parent tmutil exclusion from older
# catalogs (manual: tmutil removeexclusion "$HOME/Library/Developer/CoreSimulator").
path|$HOME/Library/Developer/CoreSimulator/Caches|Simulator caches
path|$HOME/Library/Developer/CoreSimulator/Temp|Simulator temp
path|$HOME/Library/Developer/CoreSimulator/Volumes|Simulator runtime images (Xcode 15+)
path|$HOME/Library/Developer/CoreSimulator/Devices|iOS Simulator device containers
# System CoreSimulator is runtime images, not user device metadata — keep whole tree.
path|/Library/Developer/CoreSimulator|System CoreSimulator

#@macOS Caches
path|$HOME/Library/Caches|App caches — auto-regenerated
path|$HOME/Library/Logs|App logs
path|/private/var/folders|macOS per-user kernel temp caches (regenerable)

#@Dev Tools
path|$HOME/.cursor/extensions|Cursor extensions — reinstallable
path|$HOME/.vscode/extensions|VS Code extensions — reinstallable
path|$HOME/.bmad/cache|BMAD cache
path|$HOME/.terraform.d/plugin-cache|Terraform plugin cache
path|$HOME/.pulumi|Pulumi cache
path|$HOME/.helm|Helm chart cache
path|$HOME/.kube/cache|kubectl cache
# JetBrains, VS Code, and VS Code Insiders cache directories under
# $HOME/Library/Caches are covered by the macOS Caches parent rule.

#@AI / LLM
path|$HOME/.cache/huggingface|Hugging Face model cache
path|$HOME/.cache/lm-studio|LM Studio model cache
path|$HOME/.ollama/models|Ollama models — re-downloadable (parent .ollama keeps id_ed25519 + history)
path|$HOME/Library/Application Support/Claude/vm_bundles|Claude Code VM images
path|$HOME/Library/Application Support/Claude/local-agent-mode-sessions|Claude agent ephemeral sessions
path|$HOME/Library/Application Support/superwhisper/models|SuperWhisper — models re-downloadable

#@IDE and Dev Tool Caches
# Selective regenerable caches under ~/Library/Application Support (#53).
# Parent app folders mix settings, keymaps, tokens, and sessions with caches
# — those roots stay in backups. $HOME/Library is a prune zone, so these
# must be static path rules (dynamic patterns never see them).
#
# Path targets may include a glob segment (e.g. JetBrains/*/plugins) so
# versioned IDE folders are covered without listing every release.

# Cursor (VS Code fork) — keep User/settings.json, keybindings, History,
# globalStorage (chat/state). workspaceStorage and Electron caches regenerate.
path|$HOME/Library/Application Support/Cursor/User/workspaceStorage|Cursor workspaceStorage — regenerable
path|$HOME/Library/Application Support/Cursor/Cache|Cursor Chromium cache
path|$HOME/Library/Application Support/Cursor/CachedData|Cursor V8 cached data
path|$HOME/Library/Application Support/Cursor/CachedExtensionVSIXs|Cursor cached VSIX downloads
path|$HOME/Library/Application Support/Cursor/Code Cache|Cursor V8 code cache
path|$HOME/Library/Application Support/Cursor/GPUCache|Cursor GPU cache

# Antigravity (VS Code-based; folder is Antigravity or Antigravity IDE)
path|$HOME/Library/Application Support/Antigravity/User/workspaceStorage|Antigravity workspaceStorage — regenerable
path|$HOME/Library/Application Support/Antigravity/Cache|Antigravity Chromium cache
path|$HOME/Library/Application Support/Antigravity/CachedData|Antigravity V8 cached data
path|$HOME/Library/Application Support/Antigravity/CachedExtensionVSIXs|Antigravity cached VSIX downloads
path|$HOME/Library/Application Support/Antigravity IDE/User/workspaceStorage|Antigravity IDE workspaceStorage — regenerable
path|$HOME/Library/Application Support/Antigravity IDE/Cache|Antigravity IDE Chromium cache
path|$HOME/Library/Application Support/Antigravity IDE/CachedData|Antigravity IDE V8 cached data
path|$HOME/Library/Application Support/Antigravity IDE/CachedExtensionVSIXs|Antigravity IDE cached VSIX downloads

# Auto Claude UI — venvs only (settings stay in the parent)
path|$HOME/Library/Application Support/auto-claude-ui/.venv|Auto Claude UI venv — regenerated
path|$HOME/Library/Application Support/auto-claude-ui/python-venv|Auto Claude UI python-venv — regenerated

# Discord — keep Local Storage / Cookies (auth tokens)
path|$HOME/Library/Application Support/discord/Cache|Discord Chromium cache
path|$HOME/Library/Application Support/discord/Code Cache|Discord V8 code cache
path|$HOME/Library/Application Support/discord/GPUCache|Discord GPU cache
path|$HOME/Library/Application Support/discord/modules|Discord Electron modules — re-downloaded

# GitKraken CLI — keep parent (auth); versions/ is cached cores
path|$HOME/Library/Application Support/GitKrakenCLI/versions|GitKraken CLI cached cores — re-downloaded
path|$HOME/Library/Application Support/GitKrakenCLI/cache|GitKraken CLI cache — regenerated

# JetBrains — plugins reinstallable; options/keymaps/scratches stay
path|$HOME/Library/Application Support/JetBrains/*/plugins|JetBrains IDE plugins — reinstallable
path|$HOME/Library/Application Support/JetBrains/Toolbox/apps|JetBrains Toolbox downloaded IDEs — reinstallable

# virtualenv tool cache (interpreter discovery / wheels — not project venvs)
path|$HOME/Library/Application Support/virtualenv|virtualenv discovery/wheel cache — regenerated

# SQLTools language server deps
path|$HOME/Library/Application Support/vscode-sqltools/node_modules|SQLTools node_modules — regenerated

# Zed — settings live in ~/.config/zed; languages/extensions/node re-download
path|$HOME/Library/Application Support/Zed/languages|Zed language servers — re-downloaded
path|$HOME/Library/Application Support/Zed/extensions|Zed extensions — reinstallable
path|$HOME/Library/Application Support/Zed/node|Zed bundled Node runtime — re-downloaded

# ── DYNAMIC SCAN PATTERNS (pattern) ──────────────────────────

#@Node.js / JavaScript
pattern|node_modules|Node.js dependencies (regenerable via npm/yarn install)
pattern|.next|Next.js build cache
pattern|.nuxt|Nuxt.js build
pattern|.turbo|Turborepo cache
pattern|.parcel-cache|Parcel cache
pattern|.svelte-kit|SvelteKit build
pattern|.astro|Astro build
pattern|.output|Build output (Nuxt/Nitro)
pattern|dist|Build distribution output
pattern|build|Build output directory

#@Python
pattern|.venv|Python virtual environment
pattern|venv|Python virtual environment
pattern|python-venv|Python virtual environment (alt)
pattern|virtualenv|Python virtual environment (alt)
pattern|.virtualenv|Python virtual environment (alt)
pattern|__pycache__|Python bytecode cache
pattern|.pytest_cache|pytest cache
pattern|.mypy_cache|mypy cache
pattern|.ruff_cache|ruff cache
pattern|.tox|tox environments
pattern|.eggs|Python egg artifacts
pattern|site-packages|Python lib/pythonX.Y/site-packages trees (regenerable pip output: tool installs, brew, pyenv, …)

#@Rust / Cargo
pattern|target|Rust/Cargo build output

#@Java / JVM
pattern|.gradle|Gradle project cache

#@Ruby / iOS
pattern|Pods|CocoaPods dependencies

#@Claude Code / Codex
pattern|.auto-claude|Auto-Claude — ephemeral worktrees/sessions
pattern|.codex|Codex — ephemeral worktrees/sessions
pattern|worktrees|git/Claude worktrees — ephemeral copies

#@Generic caches
# pattern|.cache|Generic cache (opt-in: too broad by default)

# ── SCAN PRUNE ZONES (prune) ─────────────────────────────────

#@Prune zones
prune|$HOME/Library|System library — subdirs handled by static rules
prune|$HOME/.Trash|Trash directory
prune|$HOME/.bun|Bun runtime — handled by static rules
# Shadow copies left by package-manager reinstalls/migrations. They are not
# static exclusions (the live tree is already covered); prune them so the
# dynamic scan does not emit one exclusion per nested dist/node_modules (#25).
prune|$HOME/.bun.bak|Bun reinstall backup — skip scan of regenerable shadow copy
prune|$HOME/.bun.old|Bun previous install — skip scan of regenerable shadow copy
prune|$HOME/.npm.bak|npm reinstall backup — skip scan of regenerable shadow copy
prune|$HOME/.npm.old|npm previous install — skip scan of regenerable shadow copy
prune|$HOME/.yarn.bak|Yarn reinstall backup — skip scan of regenerable shadow copy
prune|$HOME/.yarn.old|Yarn previous install — skip scan of regenerable shadow copy
prune|$HOME/.pnpm-store.bak|pnpm store backup — skip scan of regenerable shadow copy
prune|$HOME/.pnpm-store.old|pnpm store previous — skip scan of regenerable shadow copy
prune|$HOME/.cargo.bak|Cargo backup — skip scan of regenerable shadow copy
prune|$HOME/.cargo.old|Cargo previous install — skip scan of regenerable shadow copy
prune|$HOME/.nvm|nvm versions — handled by static rules
prune|$HOME/.cursor/extensions|Cursor extensions — handled by static rules
prune|$HOME/.vscode/extensions|VS Code extensions — handled by static rules
prune|$HOME/.local/share/uv|uv data — handled by static rules
prune|$HOME/.local/pipx|pipx venvs — handled by static rules
prune|$HOME/.bmad/cache|BMAD cache — handled by static rules
prune|$HOME/.ollama/models|Ollama models — handled by static rules
