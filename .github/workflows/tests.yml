name: Tests

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

permissions:
  contents: read

jobs:
  test:
    name: Smoke tests
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6

      - name: Run smoke tests
        run: make test
