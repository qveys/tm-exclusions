name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    name: Smoke tests
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6

      - name: Run smoke tests
        run: make test
