name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Smoke tests
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run smoke tests
        run: make test
