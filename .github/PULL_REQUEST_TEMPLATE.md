<!--
Thanks for the PR! Please fill in the sections below.
Title: emoji + Conventional Commits, e.g. "✨ feat(config): add Foo cache rule"
-->

## Summary

<!-- 1–3 bullets — the *why* and the *what*. -->

-

## Test plan

<!-- How did you verify this? Tick what applies and add specifics. -->

- [ ] `make lint` passes
- [ ] `make test` passes (smoke tests, `--dry-run` only)
- [ ] Tested manually on macOS (`tm-exclusions --dry-run`)
- [ ] No new external runtime dependencies
- [ ] Bash 3.2 compatible (no `${var,,}`, no associative arrays, no `mapfile`)

## Notes for the reviewer

<!-- Hidden constraints, follow-ups, related issues. -->

- Closes #
